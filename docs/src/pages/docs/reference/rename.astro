---
import DocsLayout from "../../../layouts/DocsLayout.astro";
---

<DocsLayout title="bao rename" description="Rename commands in your CLI">
  <div class="mb-8 md:mb-12">
    <h1 class="text-2xl md:text-4xl font-arcade text-white mb-4">
      bao rename
    </h1>
    <p class="text-gray-400 text-base md:text-lg leading-relaxed">
      Rename commands in your <code class="text-arcade-yellow">bao.toml</code> and automatically update handler files.
    </p>
  </div>

  <!-- Usage -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-yellow mb-6 pb-2 border-b border-arcade-yellow/30">
      // USAGE
    </h2>

    <div class="border-2 border-arcade-yellow/50 rounded-lg overflow-hidden mb-6">
      <div class="bg-black px-4 py-2 border-b border-arcade-yellow/30">
        <span class="font-arcade text-[10px] text-arcade-yellow">syntax</span>
      </div>
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code class="text-arcade-lime">bao rename command &lt;OLD_NAME&gt; &lt;NEW_NAME&gt; [OPTIONS]</code></pre>
    </div>
  </section>

  <!-- Options -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-cyan mb-6 pb-2 border-b border-arcade-cyan/30">
      // OPTIONS
    </h2>

    <div class="overflow-x-auto">
      <table class="w-full text-sm border border-gray-700">
        <thead>
          <tr class="bg-black/50 font-arcade text-[10px]">
            <th class="text-left p-3 text-arcade-yellow border-b border-gray-700">OPTION</th>
            <th class="text-left p-3 text-arcade-yellow border-b border-gray-700">DESCRIPTION</th>
          </tr>
        </thead>
        <tbody class="text-gray-400">
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">-c, --config &lt;PATH&gt;</code></td>
            <td class="p-3">Path to bao.toml (defaults to ./bao.toml)</td>
          </tr>
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">-o, --output &lt;PATH&gt;</code></td>
            <td class="p-3">Output directory containing src/handlers (defaults to .)</td>
          </tr>
          <tr>
            <td class="p-3"><code class="text-arcade-cyan">-h, --help</code></td>
            <td class="p-3">Print help information</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- Examples -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-pink mb-6 pb-2 border-b border-arcade-pink/30">
      // EXAMPLES
    </h2>

    <div class="space-y-6">
      <div>
        <p class="text-gray-400 mb-3">Rename a top-level command:</p>
        <div class="border border-arcade-pink/30 rounded overflow-hidden">
          <pre class="p-4 text-sm bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao rename command deploy publish</span>
<span class="text-gray-500">Renamed command 'deploy' to 'publish'</span>
<span class="text-gray-500">  src/handlers/deploy.rs -> src/handlers/publish.rs</span></code></pre>
        </div>
      </div>

      <div>
        <p class="text-gray-400 mb-3">Rename a subcommand (use <code class="text-arcade-cyan">/</code> as separator):</p>
        <div class="border border-arcade-pink/30 rounded overflow-hidden">
          <pre class="p-4 text-sm bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao rename command users/create users/add</span>
<span class="text-gray-500">Renamed command 'users/create' to 'users/add'</span>
<span class="text-gray-500">  src/handlers/users/create.rs -> src/handlers/users/add.rs</span></code></pre>
        </div>
      </div>
    </div>
  </section>

  <!-- What Gets Updated -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-lime mb-6 pb-2 border-b border-arcade-lime/30">
      // WHAT GETS UPDATED
    </h2>

    <ul class="space-y-3 text-gray-400">
      <li class="flex items-start gap-3">
        <span class="text-arcade-lime">▸</span>
        <div>
          <span class="text-white">bao.toml</span> — The command section header is renamed (e.g., <code class="text-arcade-cyan">[commands.deploy]</code> → <code class="text-arcade-cyan">[commands.publish]</code>)
        </div>
      </li>
      <li class="flex items-start gap-3">
        <span class="text-arcade-lime">▸</span>
        <div>
          <span class="text-white">Handler file</span> — The handler file or directory is renamed to match
        </div>
      </li>
      <li class="flex items-start gap-3">
        <span class="text-arcade-lime">▸</span>
        <div>
          <span class="text-white">Nested sections</span> — Any nested command sections are also updated
        </div>
      </li>
    </ul>
  </section>

  <!-- Constraints -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-yellow mb-6 pb-2 border-b border-arcade-yellow/30">
      // CONSTRAINTS
    </h2>

    <div class="border border-arcade-yellow/30 rounded-lg p-4 bg-arcade-dark">
      <ul class="space-y-2 text-gray-400">
        <li class="flex items-start gap-2">
          <span class="text-arcade-yellow">!</span>
          <span>Commands can only be renamed at the same level (cannot move between parents)</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="text-arcade-yellow">!</span>
          <span>New name must not conflict with existing commands</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="text-arcade-yellow">!</span>
          <span>New name cannot be a Rust reserved keyword</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="text-arcade-yellow">!</span>
          <span>Name must start with a letter or underscore, contain only alphanumeric characters, underscores, and dashes</span>
        </li>
      </ul>
    </div>

    <p class="text-gray-500 text-sm mt-4">
      To move a command to a different parent, use <code class="text-arcade-cyan">bao remove</code> followed by <code class="text-arcade-cyan">bao add</code>.
    </p>
  </section>

  <!-- Navigation -->
  <div class="mt-12 pt-8 border-t border-gray-800 flex justify-between items-center">
    <a href="/docs/reference/remove" class="inline-flex items-center gap-2 text-gray-400 hover:text-white font-arcade text-sm">
      <span>&larr;</span> bao remove
    </a>
    <a href="/docs/reference/list" class="inline-flex items-center gap-2 text-arcade-pink hover:underline font-arcade text-sm">
      bao list <span>&rarr;</span>
    </a>
  </div>
</DocsLayout>
