---
import DocsLayout from "../../../layouts/DocsLayout.astro";
---

<DocsLayout title="bao bake" description="Generate code from bao.toml">
  <div class="mb-8 md:mb-12">
    <h1 class="text-2xl md:text-4xl font-arcade text-white mb-4">
      bao bake
    </h1>
    <p class="text-gray-400 text-base md:text-lg leading-relaxed">
      Generate code from <code class="text-arcade-yellow">bao.toml</code>. Run this after every manifest change.
    </p>
  </div>

  <!-- Usage -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-pink mb-6 pb-2 border-b border-arcade-pink/30">
      // USAGE
    </h2>

    <div class="border-2 border-arcade-pink/50 rounded-lg overflow-hidden mb-6">
      <div class="bg-black px-4 py-2 border-b border-arcade-pink/30">
        <span class="font-arcade text-[10px] text-arcade-pink">syntax</span>
      </div>
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code class="text-arcade-lime">bao bake [OPTIONS]</code></pre>
    </div>
  </section>

  <!-- Example -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-cyan mb-6 pb-2 border-b border-arcade-cyan/30">
      // EXAMPLE
    </h2>

    <div class="border-2 border-arcade-cyan/50 rounded-lg overflow-hidden">
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao bake</span>
<span class="text-gray-500">  Generated src/cli.rs
  Generated src/commands/deploy.rs
  Generated src/commands/mod.rs

  Done! 3 files generated.</span></code></pre>
    </div>
  </section>

  <!-- Options -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-yellow mb-6 pb-2 border-b border-arcade-yellow/30">
      // OPTIONS
    </h2>

    <div class="overflow-x-auto">
      <table class="w-full text-sm border border-gray-700">
        <thead>
          <tr class="bg-black/50 font-arcade text-[10px]">
            <th class="text-left p-3 text-arcade-pink border-b border-gray-700">OPTION</th>
            <th class="text-left p-3 text-arcade-pink border-b border-gray-700">DESCRIPTION</th>
          </tr>
        </thead>
        <tbody class="text-gray-400">
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">-p, --path &lt;PATH&gt;</code></td>
            <td class="p-3">Path to bao.toml (default: current directory)</td>
          </tr>
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">--dry-run</code></td>
            <td class="p-3">Preview without writing files</td>
          </tr>
          <tr>
            <td class="p-3"><code class="text-arcade-cyan">-h, --help</code></td>
            <td class="p-3">Print help information</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- Handler Preservation -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-lime mb-6 pb-2 border-b border-arcade-lime/30">
      // HANDLER PRESERVATION
    </h2>

    <div class="bg-arcade-lime/10 border border-arcade-lime/30 p-4 rounded mb-6">
      <p class="text-arcade-lime text-sm font-arcade mb-2">SAFE TO RUN</p>
      <p class="text-gray-400 text-sm">
        Your handler code in <code class="text-arcade-cyan">src/commands/</code> is never overwritten. Only the CLI structure is regenerated.
      </p>
    </div>

    <p class="text-gray-400 mb-4">Files that are regenerated:</p>
    <ul class="space-y-2 text-gray-400 mb-4">
      <li class="flex items-center gap-2">
        <span class="text-arcade-pink">+</span> <code class="text-arcade-cyan">src/cli.rs</code> - CLI structure and argument parsing
      </li>
      <li class="flex items-center gap-2">
        <span class="text-arcade-pink">+</span> <code class="text-arcade-cyan">src/commands/mod.rs</code> - Module exports
      </li>
      <li class="flex items-center gap-2">
        <span class="text-arcade-pink">+</span> <code class="text-arcade-cyan">src/main.rs</code> - Entry point
      </li>
    </ul>

    <p class="text-gray-400">Files that are preserved:</p>
    <ul class="space-y-2 text-gray-400">
      <li class="flex items-center gap-2">
        <span class="text-arcade-lime">+</span> <code class="text-arcade-cyan">src/commands/*.rs</code> - Your handler implementations
      </li>
      <li class="flex items-center gap-2">
        <span class="text-arcade-lime">+</span> <code class="text-arcade-cyan">src/context.rs</code> - Your context definition
      </li>
    </ul>
  </section>

  <!-- More Examples -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-purple mb-6 pb-2 border-b border-arcade-purple/30">
      // MORE EXAMPLES
    </h2>

    <div class="border border-arcade-purple/30 rounded overflow-hidden">
      <pre class="p-4 text-sm bg-arcade-dark"><code><span class="text-gray-500"># Generate code</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao bake</span>

<span class="text-gray-500"># Preview only (no file writes)</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao bake --dry-run</span>

<span class="text-gray-500"># Use a different manifest path</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao bake -p ./other/project</span></code></pre>
    </div>
  </section>

  <!-- Navigation -->
  <div class="mt-12 pt-8 border-t border-gray-800 flex justify-between items-center">
    <a href="/docs/reference/init" class="inline-flex items-center gap-2 text-gray-400 hover:text-white font-arcade text-sm">
      <span>&larr;</span> bao init
    </a>
    <a href="/docs/reference/add" class="inline-flex items-center gap-2 text-arcade-yellow hover:underline font-arcade text-sm">
      bao add <span>&rarr;</span>
    </a>
  </div>
</DocsLayout>
