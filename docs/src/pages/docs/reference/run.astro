---
import DocsLayout from "../../../layouts/DocsLayout.astro";
---

<DocsLayout title="bao run" description="Build and run your CLI">
  <div class="mb-8 md:mb-12">
    <h1 class="text-2xl md:text-4xl font-arcade text-white mb-4">
      bao run
    </h1>
    <p class="text-gray-400 text-base md:text-lg leading-relaxed">
      Build and run your CLI. Uses <code class="text-arcade-cyan">cargo run</code> for Rust, <code class="text-arcade-cyan">bun run</code> for TypeScript.
    </p>
  </div>

  <!-- Usage -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-pink mb-6 pb-2 border-b border-arcade-pink/30">
      // USAGE
    </h2>

    <div class="border-2 border-arcade-pink/50 rounded-lg overflow-hidden mb-6">
      <div class="bg-black px-4 py-2 border-b border-arcade-pink/30">
        <span class="font-arcade text-[10px] text-arcade-pink">syntax</span>
      </div>
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code class="text-arcade-lime">bao run [OPTIONS] [-- &lt;ARGS&gt;...]</code></pre>
    </div>

    <div class="bg-arcade-pink/10 border border-arcade-pink/30 p-3 rounded text-sm">
      <p class="text-gray-400"><span class="text-arcade-pink font-semibold">Important:</span> Use <code class="text-arcade-cyan">--</code> to separate bao options from your CLI arguments.</p>
    </div>
  </section>

  <!-- Example -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-cyan mb-6 pb-2 border-b border-arcade-cyan/30">
      // EXAMPLE
    </h2>

    <div class="border-2 border-arcade-cyan/50 rounded-lg overflow-hidden">
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao run -- deploy production --force</span>
<span class="text-gray-500">   Compiling myapp v1.0.0
    Finished dev target(s)
     Running `target/debug/myapp deploy production --force`</span>
<span class="text-gray-400">Deploying to production (force mode)...</span></code></pre>
    </div>
  </section>

  <!-- Options -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-yellow mb-6 pb-2 border-b border-arcade-yellow/30">
      // OPTIONS
    </h2>

    <div class="overflow-x-auto">
      <table class="w-full text-sm border border-gray-700">
        <thead>
          <tr class="bg-black/50 font-arcade text-[10px]">
            <th class="text-left p-3 text-arcade-pink border-b border-gray-700">OPTION</th>
            <th class="text-left p-3 text-arcade-pink border-b border-gray-700">DESCRIPTION</th>
          </tr>
        </thead>
        <tbody class="text-gray-400">
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">-p, --path &lt;PATH&gt;</code></td>
            <td class="p-3">Path to bao.toml</td>
          </tr>
          <tr>
            <td class="p-3"><code class="text-arcade-cyan">-h, --help</code></td>
            <td class="p-3">Print help information</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- More Examples -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-lime mb-6 pb-2 border-b border-arcade-lime/30">
      // MORE EXAMPLES
    </h2>

    <div class="border border-arcade-lime/30 rounded overflow-hidden">
      <pre class="p-4 text-sm bg-arcade-dark"><code><span class="text-gray-500"># Run with no arguments</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao run</span>

<span class="text-gray-500"># Show your CLI's help</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao run -- --help</span>

<span class="text-gray-500"># Run a command with arguments</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao run -- deploy prod --force</span>

<span class="text-gray-500"># Run a nested command</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao run -- db migrate --dry-run</span></code></pre>
    </div>
  </section>

  <!-- How It Works -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-purple mb-6 pb-2 border-b border-arcade-purple/30">
      // HOW IT WORKS
    </h2>

    <p class="text-gray-400 mb-4">
      <code class="text-arcade-cyan">bao run</code> detects your project language and runs the appropriate build command:
    </p>

    <div class="grid md:grid-cols-2 gap-4">
      <div class="border border-arcade-pink/50 bg-black/30 p-4">
        <p class="font-arcade text-arcade-pink text-xs mb-3">RUST</p>
        <p class="text-gray-400 text-sm mb-2">Runs:</p>
        <pre class="text-sm text-gray-300"><code>cargo run -- &lt;ARGS&gt;</code></pre>
      </div>
      <div class="border border-arcade-cyan/50 bg-black/30 p-4">
        <p class="font-arcade text-arcade-cyan text-xs mb-3">TYPESCRIPT</p>
        <p class="text-gray-400 text-sm mb-2">Runs:</p>
        <pre class="text-sm text-gray-300"><code>bun run src/index.ts -- &lt;ARGS&gt;</code></pre>
      </div>
    </div>
  </section>

  <!-- Navigation -->
  <div class="mt-12 pt-8 border-t border-gray-800 flex justify-between items-center">
    <a href="/docs/reference/fmt" class="inline-flex items-center gap-2 text-gray-400 hover:text-white font-arcade text-sm">
      <span>&larr;</span> bao fmt
    </a>
    <a href="/docs/reference/completions" class="inline-flex items-center gap-2 text-arcade-pink hover:underline font-arcade text-sm">
      bao completions <span>&rarr;</span>
    </a>
  </div>
</DocsLayout>
