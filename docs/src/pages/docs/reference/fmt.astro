---
import DocsLayout from "../../../layouts/DocsLayout.astro";
---

<DocsLayout title="bao fmt" description="Format bao.toml configuration file">
  <div class="mb-8 md:mb-12">
    <h1 class="text-2xl md:text-4xl font-arcade text-white mb-4">
      bao fmt
    </h1>
    <p class="text-gray-400 text-base md:text-lg leading-relaxed">
      Format your <code class="text-arcade-yellow">bao.toml</code> configuration file.
    </p>
  </div>

  <!-- Usage -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-yellow mb-6 pb-2 border-b border-arcade-yellow/30">
      // USAGE
    </h2>

    <div class="border-2 border-arcade-yellow/50 rounded-lg overflow-hidden mb-6">
      <div class="bg-black px-4 py-2 border-b border-arcade-yellow/30">
        <span class="font-arcade text-[10px] text-arcade-yellow">syntax</span>
      </div>
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code class="text-arcade-lime">bao fmt [OPTIONS]</code></pre>
    </div>
  </section>

  <!-- Options -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-cyan mb-6 pb-2 border-b border-arcade-cyan/30">
      // OPTIONS
    </h2>

    <div class="overflow-x-auto">
      <table class="w-full text-sm border border-gray-700">
        <thead>
          <tr class="bg-black/50 font-arcade text-[10px]">
            <th class="text-left p-3 text-arcade-yellow border-b border-gray-700">OPTION</th>
            <th class="text-left p-3 text-arcade-yellow border-b border-gray-700">DESCRIPTION</th>
          </tr>
        </thead>
        <tbody class="text-gray-400">
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">--check</code></td>
            <td class="p-3">Check if file is formatted without making changes (exits with code 1 if not formatted)</td>
          </tr>
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">-c, --config &lt;PATH&gt;</code></td>
            <td class="p-3">Path to bao.toml (defaults to ./bao.toml)</td>
          </tr>
          <tr>
            <td class="p-3"><code class="text-arcade-cyan">-h, --help</code></td>
            <td class="p-3">Print help information</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- Examples -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-pink mb-6 pb-2 border-b border-arcade-pink/30">
      // EXAMPLES
    </h2>

    <div class="space-y-6">
      <div>
        <p class="text-gray-400 mb-3">Format the configuration file:</p>
        <div class="border border-arcade-pink/30 rounded overflow-hidden">
          <pre class="p-4 text-sm bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao fmt</span>
<span class="text-gray-500">Formatted bao.toml</span></code></pre>
        </div>
      </div>

      <div>
        <p class="text-gray-400 mb-3">Check formatting in CI:</p>
        <div class="border border-arcade-pink/30 rounded overflow-hidden">
          <pre class="p-4 text-sm bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao fmt --check</span>
<span class="text-gray-500">bao.toml is formatted</span>

<span class="text-gray-500"># If not formatted:</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao fmt --check</span>
<span class="text-arcade-pink">error: bao.toml is not formatted</span>
<span class="text-gray-500">Run `bao fmt` to fix.</span></code></pre>
        </div>
      </div>
    </div>
  </section>

  <!-- CI Integration -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-lime mb-6 pb-2 border-b border-arcade-lime/30">
      // CI INTEGRATION
    </h2>

    <p class="text-gray-400 mb-4">
      Use <code class="text-arcade-cyan">bao fmt --check</code> in your CI pipeline to ensure consistent formatting:
    </p>

    <div class="border border-arcade-lime/30 rounded overflow-hidden">
      <div class="bg-black px-4 py-2 border-b border-arcade-lime/30">
        <span class="font-arcade text-[10px] text-arcade-lime">.github/workflows/ci.yml</span>
      </div>
      <pre class="p-4 text-sm bg-arcade-dark"><code><span class="text-arcade-yellow">- name:</span> Check bao.toml formatting
  <span class="text-arcade-yellow">run:</span> bao fmt --check</code></pre>
    </div>
  </section>

  <!-- Navigation -->
  <div class="mt-12 pt-8 border-t border-gray-800 flex justify-between items-center">
    <a href="/docs/reference/clean" class="inline-flex items-center gap-2 text-gray-400 hover:text-white font-arcade text-sm">
      <span>&larr;</span> bao clean
    </a>
    <a href="/docs/reference/run" class="inline-flex items-center gap-2 text-arcade-pink hover:underline font-arcade text-sm">
      bao run <span>&rarr;</span>
    </a>
  </div>
</DocsLayout>
