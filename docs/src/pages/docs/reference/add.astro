---
import DocsLayout from "../../../layouts/DocsLayout.astro";
---

<DocsLayout title="bao add" description="Add commands, arguments, flags, or context fields">
  <div class="mb-8 md:mb-12">
    <h1 class="text-2xl md:text-4xl font-arcade text-white mb-4">
      bao add
    </h1>
    <p class="text-gray-400 text-base md:text-lg leading-relaxed">
      Add commands, arguments, flags, or context fields to <code class="text-arcade-yellow">bao.toml</code>.
    </p>
  </div>

  <!-- Usage -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-yellow mb-6 pb-2 border-b border-arcade-yellow/30">
      // USAGE
    </h2>

    <div class="border-2 border-arcade-yellow/50 rounded-lg overflow-hidden mb-6">
      <div class="bg-black px-4 py-2 border-b border-arcade-yellow/30">
        <span class="font-arcade text-[10px] text-arcade-yellow">syntax</span>
      </div>
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code class="text-arcade-lime">bao add &lt;SUBCOMMAND&gt;</code></pre>
    </div>
  </section>

  <!-- Subcommands -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-cyan mb-6 pb-2 border-b border-arcade-cyan/30">
      // SUBCOMMANDS
    </h2>

    <div class="overflow-x-auto">
      <table class="w-full text-sm border border-gray-700">
        <thead>
          <tr class="bg-black/50 font-arcade text-[10px]">
            <th class="text-left p-3 text-arcade-yellow border-b border-gray-700">SUBCOMMAND</th>
            <th class="text-left p-3 text-arcade-yellow border-b border-gray-700">DESCRIPTION</th>
          </tr>
        </thead>
        <tbody class="text-gray-400">
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">command &lt;NAME&gt;</code></td>
            <td class="p-3">Add a new command</td>
          </tr>
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">arg &lt;COMMAND&gt; &lt;NAME&gt;</code></td>
            <td class="p-3">Add an argument to a command</td>
          </tr>
          <tr class="border-b border-gray-800">
            <td class="p-3"><code class="text-arcade-cyan">flag &lt;COMMAND&gt; &lt;NAME&gt;</code></td>
            <td class="p-3">Add a flag to a command</td>
          </tr>
          <tr>
            <td class="p-3"><code class="text-arcade-cyan">context &lt;NAME&gt;</code></td>
            <td class="p-3">Add a context field</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- Add Command -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-pink mb-6 pb-2 border-b border-arcade-pink/30">
      // ADD COMMAND
    </h2>

    <div class="border-2 border-arcade-pink/50 rounded-lg overflow-hidden mb-6">
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao add command deploy</span>
<span class="text-gray-500">  Added command "deploy" to bao.toml</span>

<span class="text-gray-500"># Nested commands use slash notation</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao add command db/migrate</span>
<span class="text-gray-500">  Added command "migrate" under "db"</span></code></pre>
    </div>
  </section>

  <!-- Add Argument -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-lime mb-6 pb-2 border-b border-arcade-lime/30">
      // ADD ARGUMENT
    </h2>

    <div class="border-2 border-arcade-lime/50 rounded-lg overflow-hidden mb-6">
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao add arg deploy environment</span>
<span class="text-gray-500">  Added argument "environment" to command "deploy"</span>

<span class="text-gray-500"># For nested commands</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao add arg db/migrate version</span>
<span class="text-gray-500">  Added argument "version" to command "db/migrate"</span></code></pre>
    </div>
  </section>

  <!-- Add Flag -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-arcade-purple mb-6 pb-2 border-b border-arcade-purple/30">
      // ADD FLAG
    </h2>

    <div class="border-2 border-arcade-purple/50 rounded-lg overflow-hidden mb-6">
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao add flag deploy force</span>
<span class="text-gray-500">  Added flag "force" to command "deploy"</span>

<span class="text-gray-500"># For nested commands</span>
<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao add flag db/migrate dry_run</span>
<span class="text-gray-500">  Added flag "dry_run" to command "db/migrate"</span></code></pre>
    </div>
  </section>

  <!-- Add Context -->
  <section class="mb-12">
    <h2 class="font-arcade text-xl text-white mb-6 pb-2 border-b border-gray-700">
      // ADD CONTEXT
    </h2>

    <div class="border border-gray-700 rounded-lg overflow-hidden mb-6">
      <pre class="p-4 text-sm overflow-x-auto bg-arcade-dark"><code><span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao add context db</span>
<span class="text-gray-500">  Added context field "db" to bao.toml</span>

<span class="text-arcade-cyan">$</span> <span class="text-arcade-lime">bao add context config</span>
<span class="text-gray-500">  Added context field "config" to bao.toml</span></code></pre>
    </div>
  </section>

  <!-- Navigation -->
  <div class="mt-12 pt-8 border-t border-gray-800 flex justify-between items-center">
    <a href="/docs/reference/bake" class="inline-flex items-center gap-2 text-gray-400 hover:text-white font-arcade text-sm">
      <span>&larr;</span> bao bake
    </a>
    <a href="/docs/reference/remove" class="inline-flex items-center gap-2 text-arcade-purple hover:underline font-arcade text-sm">
      bao remove <span>&rarr;</span>
    </a>
  </div>
</DocsLayout>
